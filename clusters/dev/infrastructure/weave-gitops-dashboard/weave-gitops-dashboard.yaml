apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  annotations:
    metadata.weave.works/description: This is the Weave GitOps Dashboard.  It provides
      a simple way to get insights into your GitOps workloads.
  name: ww-gitops
  namespace: flux-system
spec:
  chart:
    spec:
      chart: weave-gitops
      sourceRef:
        kind: HelmRepository
        name: ww-gitops
  interval: 10m
  values:
    ingress:
      enabled: true
      className: "nginx"
      hosts:
        - host: weave.localhost
          paths:
            - path: /
              pathType: ImplementationSpecific
    adminUser:
      create: true
      passwordHash: $2a$10$DY7sxB/bC5uv7zeTbm.5OOiDwC9Jdl0NvAz01MVHENABzOi.5YXaG
      username: admin

